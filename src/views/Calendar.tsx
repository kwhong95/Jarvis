import styled from "@emotion/styled";
import { UserWrap } from "wrapper";

const Container = styled.div``;

const Calendar = () => {
  return <Container>Calendar</Container>;
};

export default UserWrap(Calendar, "Javis | Calendar");
