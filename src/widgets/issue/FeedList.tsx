import styled from "@emotion/styled";

const Container = styled.div`
  padding: 1rem;
  height: 100%;
  display: flex;
  justify-content: center;
  align-items: center;
`;

const FeedList = () => {
  return <Container>등록된 피드가 없습니다.</Container>;
};

export default FeedList;
